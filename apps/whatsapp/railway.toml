[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=apps/whatsapp"

[deploy]
startCommand = "npm run start --workspace=apps/whatsapp"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
